# Telegram Bot Token (get it from @BotFather on Telegram)
TELEGRAM_BOT_TOKEN=your_bot_token_from_botfather

# Webhook server (for receiving Charlie responses)
WEBHOOK_PORT=3000
WEBHOOK_BASE_URL=https://your-bot-public-url.com

# Charlie Project API
CHARLIE_API_URL=https://charlie-back.cloud-station.io
CHARLIE_PROJECT_ID=your_project_id

# Authentication: use ONE of the two methods below

# Method 1: USER_TOKEN (for local testing -- bypasses API key auth)
# USER_TOKEN=your_user_token

# Method 2: API Key + Client ID (for production / CloudStation)
CHARLIE_API_KEY=your_api_key
CHARLIE_CLIENT_ID=your_client_id

# Optional: specify an agent template for Charlie to use
CHARLIE_AGENT_TEMPLATE_ID=

# Optional: Telegram chat ID to forward escalations to.
# When the agent emits [ESCALATE: <summary>] at the end of a reply,
# the marker is stripped from the customer message and a one-message
# summary is sent to this chat. Leave empty to disable escalation forwarding.
ESCALATION_CHAT_ID=
